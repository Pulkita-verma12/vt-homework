import { CustompipelinePipe } from './custompipeline-pipe';

describe('CustompipelinePipe', () => {
  it('create an instance', () => {
    const pipe = new CustompipelinePipe();
    expect(pipe).toBeTruthy();
  });
});

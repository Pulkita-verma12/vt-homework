import { CapitalisePipePipe } from './capitalise-pipe-pipe';

describe('CapitalisePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalisePipePipe();
    expect(pipe).toBeTruthy();
  });
});

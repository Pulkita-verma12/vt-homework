import { TruncateCustomePipe } from './truncate-custome-pipe';

describe('TruncateCustomePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateCustomePipe();
    expect(pipe).toBeTruthy();
  });
});
